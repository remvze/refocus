---
import { Container } from './container';
---

<section class="about">
  <Container>
    <h2 class="title">What is Pomoro?</h2>
    <p class="desc">
      Pomoro is a dead-simple Pomodoro timer in the browser. It&apos;s
      completely ad-free, open-source, and self-hostable. Everything is stored
      in your browser, so there is no need for an account.
    </p>
    <p class="desc support">
      Enjoy Pomoro?
      <a
        href="https://buymeacoffee.com/remvze"
        rel="noreferrer"
        target="_blank"
      >
        Support with a donation!
      </a>
    </p>
  </Container>
</section>

<style>
  .about {
    margin-top: 48px;

    & .title {
      margin-bottom: 12px;
      font-family: var(--font-display);
      font-size: var(--font-lg);
      line-height: 1;
    }

    & .desc {
      line-height: 1.65;
      color: var(--color-foreground-subtle);

      &:not(:last-of-type) {
        margin-bottom: 12px;
      }

      &.support {
        font-size: var(--font-sm);

        & a {
          font-weight: 500;
          color: var(--color-foreground);
          text-decoration: none;
        }
      }
    }
  }
</style>
